unit StatisticsScreen;
interface
	procedure ShowStatisticsScreen(sellsPath, productsPath : string);

implementation
        uses crt;

	(*Show statiscs screen with all information about statiscs file generation*)
	procedure ShowStatisticsScreen(sellsPath, productsPath : string);
	begin
		Writeln('Esta funcao nao esta implementada ainda!');
		Writeln('Pressione qualquer tecla para retornar ao menu anterior.');
		Readkey;
	end;
end.
